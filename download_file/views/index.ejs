<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <% if (messages) { %>
                    <% for (var i = 0; i < messages.length; i++) { %>
                        <b><%= messages[i].msg || messages[i] %></b>    
                    <% } %>
                <% } %>     
            </div>
        </div> 
    </div>
</body>
</html>